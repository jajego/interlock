generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model SpikeApplication {
  id      String @id
  state   String
  version BigInt
  @@map("spike_applications")
}
